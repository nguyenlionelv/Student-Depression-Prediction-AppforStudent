FROM nginx:alpine

COPY nginx.conf /etc/nginx/conf.d/default.conf

COPY static/ /usr/share/nginx/html/

ENV API_BASE="http://localhost:8000"

CMD /bin/sh -c "echo \"window.RUNTIME_API_BASE = '${API_BASE}';\" > /usr/share/nginx/html/env.js && nginx -g 'daemon off;'"
